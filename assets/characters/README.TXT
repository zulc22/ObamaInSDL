These .xcf source files are not to be distributed with a binary release.
Binary releases should only contain the characters folder and it's subfolders,
and not any files inside `characters/`.
This means discluding `characters/*.xcf` and `characters/README.TXT`.